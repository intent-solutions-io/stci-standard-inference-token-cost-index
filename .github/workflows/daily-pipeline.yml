name: Daily Pipeline

on:
  schedule:
    # Run daily at 00:30 UTC
    - cron: '30 0 * * *'
  workflow_dispatch:
    inputs:
      date:
        description: 'Date to collect (YYYY-MM-DD, default: today)'
        required: false
        type: string

jobs:
  collect-and-index:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Determine date
        id: date
        run: |
          if [ -n "${{ inputs.date }}" ]; then
            echo "date=${{ inputs.date }}" >> $GITHUB_OUTPUT
          else
            echo "date=$(date -u +%Y-%m-%d)" >> $GITHUB_OUTPUT
          fi

      - name: Run collector
        run: |
          python -m services.collector.pipeline --date ${{ steps.date.outputs.date }}

      - name: Run indexer
        run: |
          python -m services.indexer.pipeline --date ${{ steps.date.outputs.date }}

      - name: Verify outputs
        run: |
          echo "=== Index ==="
          cat data/indices/${{ steps.date.outputs.date }}.json | python -c "
          import sys, json
          d = json.load(sys.stdin)
          print(f\"Date: {d['date']}\")
          print(f\"Observations: {d['observation_count']}\")
          for name, data in d['indices'].items():
              print(f\"  {name}: \${data['blended_rate']:.2f}/1M ({data['model_count']} models)\")
          "

      - name: Upload to Firestore
        if: ${{ secrets.GCP_SA_KEY != '' }}
        env:
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GCP_SA_KEY }}
        run: |
          # Write credentials to temp file
          echo "$GOOGLE_APPLICATION_CREDENTIALS_JSON" > /tmp/gcp-key.json
          export GOOGLE_APPLICATION_CREDENTIALS=/tmp/gcp-key.json

          python -c "
          from services.storage.firestore import FirestoreStorage
          import json

          storage = FirestoreStorage()

          # Upload index
          with open('data/indices/${{ steps.date.outputs.date }}.json') as f:
              storage.write_index('${{ steps.date.outputs.date }}', json.load(f))
          print('Uploaded index to Firestore')

          # Upload observations
          observations = []
          with open('data/observations/${{ steps.date.outputs.date }}.jsonl') as f:
              for line in f:
                  observations.append(json.loads(line))
          storage.write_observations('${{ steps.date.outputs.date }}', observations)
          print(f'Uploaded {len(observations)} observations to Firestore')
          "

          # Clean up credentials
          rm /tmp/gcp-key.json

      - name: Commit data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --staged --quiet || git commit -m "Daily update: ${{ steps.date.outputs.date }}"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: stci-${{ steps.date.outputs.date }}
          path: |
            data/indices/${{ steps.date.outputs.date }}.json
            data/observations/${{ steps.date.outputs.date }}.jsonl
          retention-days: 30
